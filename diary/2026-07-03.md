# Daily diary: 2026-07-03

<span class="meta">The hub goes live — PR #8 merged, v0.1 released, News feed added — and the obot GitHub App requirement moves into design.</span>

## Work completed

- Merged [PR #8](https://github.com/jwildfire/obot.roadmap/pull/8) (hub migration: diary
  archive, reports, dashboard/roadmap generators, Pages deploy) after applying the 🍊😺
  brand bar and full-width header/footer theming — the site is live at
  [jwildfire.github.io/obot.roadmap](https://jwildfire.github.io/obot.roadmap/).
- Cut [v0.1 — Roadmap hub established](https://github.com/jwildfire/obot.roadmap/releases/tag/v0.1).
- Added the **News feed** (`news.html` via `scripts/build_news.mjs` — blog posts, diary
  entries, agent artifacts, and releases with type/month filters) and a persistent
  Home/Dashboard/Roadmap/News/GitHub nav with current-page marking.
- **@jwildfire approved Requirement [#3](https://github.com/jwildfire/obot.roadmap/issues/3)
  (obot GitHub App)**, advancing it to Design. Drafted the design document
  (`requirements/design/3_design.html`): one app under @jwildfire, two token flows (local
  agent helper + `actions/create-github-app-token`), minimal permission set, Keychain +
  Actions-secrets key management, and the obot-claw credential-retirement checklist.
  Verified two constraints against GitHub docs: app names can't collide with existing
  accounts (blocking `obot`/`obot-claw`/`orangebot` — `obotclaw` is free and recommended),
  and apps get no user-Projects permission, so `ROADMAP_TOKEN` stays.

## PRs / issues touched

- Merged: [PR #8](https://github.com/jwildfire/obot.roadmap/pull/8).
- Advanced: [#3](https://github.com/jwildfire/obot.roadmap/issues/3) — requirement approved;
  design doc drafted and Design section populated.

## Blockers / risks

- The `ROADMAP_TOKEN` secret is still unset, so the dashboard sign-off gates and roadmap
  Status grouping run degraded.
- The local `gh` token lacks `read:project`, so Project Status fields can't be updated from
  sessions (`gh auth refresh -s read:project` would fix it).

## 🙋 ToDo

- @jwildfire: resolve design decisions D1–D4 on [#3](https://github.com/jwildfire/obot.roadmap/issues/3)
  (app name, permission set, key storage, helper home) and 👍 for design sign-off.
- @jwildfire: review the design-doc PR for #3.
- @jwildfire: 👍 [#7](https://github.com/jwildfire/obot.roadmap/issues/7) for its (already
  implemented) design sign-off; add the `ROADMAP_TOKEN` secret.
