# Daily diary: 2026-07-04

<span class="meta">obotclaw is born — design #3 approved, the GitHub App registered and installed, and the first bot-attributed comment posted from a working session.</span>

## Work completed

- **Design #3 approved.** @jwildfire resolved D1–D4 in a working session: name
  `obotclaw`, the **broader** permission set (Contents/Issues/PRs RW + Workflows RW +
  Actions read — anticipating the `@claude` PR-responder follow-on; Discussions RW added
  at registration), Keychain + Actions-secrets key management, script + skill in
  `safety.agent`. Doc updated in [PR #14](https://github.com/jwildfire/obot.roadmap/pull/14);
  resolutions recorded on [#3](https://github.com/jwildfire/obot.roadmap/issues/3).
- **Registered the `obotclaw` GitHub App** (App ID 4215246) under @jwildfire — form
  driven via browser automation, sudo-auth and key generation by Jeremy — and installed
  it on the five whitelisted repos (installation 144370633; bot user ID 299836032).
- **Credentials stored per D3:** PEM base64-wrapped into the macOS Keychain (service
  `obot-github-app`; raw multiline values don't survive `security -w` round-trips),
  `OBOT_APP_ID`/`OBOT_APP_PRIVATE_KEY` Actions secrets set on obot.roadmap, downloaded
  PEM deleted. Token minting verified end-to-end: 1-hour token scoped to exactly the
  five repos.
- **Agent-flow acceptance met:** implemented `scripts/obot-app-token` +
  the `obot-identity` skill in safety.agent; the script's first minted token posted
  [a comment on #3](https://github.com/jwildfire/obot.roadmap/issues/3#issuecomment-4882052314)
  authored by **obotclaw[bot]**.
- Added `app-smoke-test.yml` (dispatch-only GHA acceptance path) and documented the
  identity-selection rules in AGENTS.md/README (this PR).
- Adopted the attribution-at-bottom convention for all drafted artifacts; retrofitted
  PR #14, the decisions comment, and issue #3's body.

## PRs / issues touched

- Opened: [PR #14](https://github.com/jwildfire/obot.roadmap/pull/14) (design decisions),
  safety.agent PR (obot-app-token + skill), this PR (smoke test + identity docs).
- Advanced: [#3](https://github.com/jwildfire/obot.roadmap/issues/3) — Design approved,
  Development underway; agent-flow acceptance criterion met.

## Blockers / risks

- GHA acceptance (smoke-test dispatch) waits on this PR merging — `workflow_dispatch`
  only works from the default branch.
- obot-claw credential retirement (§7) not yet run: needs a @jwildfire sign-in to the
  obot-claw account to delete its PATs; local `gh auth` audit and old-config scan pending.

## Needs @jwildfire

- Merge sign-offs: PR #14, the safety.agent PR, and this PR.
- Dispatch `app-smoke-test.yml` after merge (or ask the agent to).
- obot-claw PAT deletion (account sign-in required).
